import { Skeleton } from '@/components/ui/skeleton';

const NavUserSkeleton = () => {
  return (
    <Skeleton className="h-12 w-full animate-pulse rounded-md" />
  );
}

export default NavUserSkeleton